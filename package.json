{
  "name": "smol-translate",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --watch run ./index.ts",
    "build": "bun build --target bun --minify --sourcemap ./index.ts --outdir ./dist",
    "start": "bun --smol run ./dist/index.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "google-translate-api-x": "^10.7.2",
    "zod": "^3.24.2"
  }
}